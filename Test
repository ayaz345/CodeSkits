import 'dart:math';

void main() {
  // Import the dart:math library which contains random number generators

  var random = Random(); 

  // Create an instance of the Random() class

  var randomNumber = random.nextInt(10) + 1;
  
  // Call the nextInt() method to generate a random integer from 0 to 9
  // Add 1 to make it from 1 to 10

  print('Random: $randomNumber');

  // Print the generated random number
}
